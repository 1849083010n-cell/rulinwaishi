# requirements.txt 完整依赖清单
streamlit==1.35.0          # 核心Web框架（与代码版本匹配）
pandas==2.2.2              # 数据处理（统计地点频率）
requests==2.32.3            # 读取GitHub文档
folium==0.16.0              # 地图可视化（报错缺失的库）
streamlit-folium==0.15.1    # folium与Streamlit的适配库
